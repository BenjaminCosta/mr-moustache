# Background photography

Place the approved section backgrounds here and reference them from
`src/data/media.ts` (`backgrounds.hero`, `backgrounds.services`,
`backgrounds.location`, `backgrounds.locationFeature`, `backgrounds.footer`).
